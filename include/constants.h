#ifndef COSNTANTS_H
#define COSNTANTS_H

#include <math.h>

/* Storing constants and other common things here */
#define EPSILON 0.000001f
#define floatEqual(f1, f2) (fabs(f1 - f2) <= EPSILON)
#endif
